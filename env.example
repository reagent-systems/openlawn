# OpenLawn Environment Configuration
# Copy this file to .env.local for local development
# Copy to .env.mobile for mobile builds with Capacitor

# =============================================================================
# FIREBASE CONFIGURATION
# =============================================================================
# Get these values from your Firebase project console
# https://console.firebase.google.com/
NEXT_PUBLIC_FIREBASE_API_KEY=your_firebase_api_key_here
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your_project_id.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your_project_id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your_project_id.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=123456789012
NEXT_PUBLIC_FIREBASE_APP_ID=1:123456789012:web:abcdef1234567890
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=G-XXXXXXXXXX

# =============================================================================
# GOOGLE MAPS API
# =============================================================================
# Get this from Google Cloud Console
# https://console.cloud.google.com/apis/credentials
# Enable: Maps JavaScript API, Directions API, Geocoding API
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# =============================================================================
# GOOGLE AI (GENKIT) API
# =============================================================================
# Get this from Google AI Studio
# https://aistudio.google.com/app/apikey
# Note: This should be kept secure and not exposed in client-side code
GOOGLE_AI_API_KEY=your_google_ai_api_key_here

# =============================================================================
# MOBILE BUILD VARIABLES (for Capacitor)
# =============================================================================
# These are used when building for mobile platforms
# They can be the same as the web versions for most APIs
FIREBASE_API_KEY=${NEXT_PUBLIC_FIREBASE_API_KEY}
FIREBASE_AUTH_DOMAIN=${NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN}
FIREBASE_PROJECT_ID=${NEXT_PUBLIC_FIREBASE_PROJECT_ID}
FIREBASE_STORAGE_BUCKET=${NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET}
FIREBASE_MESSAGING_SENDER_ID=${NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID}
FIREBASE_APP_ID=${NEXT_PUBLIC_FIREBASE_APP_ID}
FIREBASE_MEASUREMENT_ID=${NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID}
GOOGLE_MAPS_API_KEY=${NEXT_PUBLIC_GOOGLE_MAPS_API_KEY}


# =============================================================================
# DEVELOPMENT SETTINGS
# =============================================================================
# Set to true to enable debug logging
NEXT_PUBLIC_DEBUG_MODE=false

# Development server port (optional)
# PORT=9002

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================
# 1. Copy this file to .env.local for local development
# 2. Copy to .env.mobile for mobile builds
# 3. Replace all "your_*_here" values with your actual API keys
# 4. Never commit .env.local or .env.mobile to version control
#
# For production deployment:
# - Set these variables in your hosting platform (Vercel, Netlify, etc.)
# - For mobile: Use the build script: npm run build:mobile
#
# Security Notes:
# - Firebase API keys are public and safe for client-side use
# - Google Maps API key should be restricted to your domains/apps
# - Google AI API key should be kept secure (use backend proxy for production) 